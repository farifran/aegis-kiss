# Issue: Implementação do EventEmitter & Barramento PubSub com Prioridades (src/eventEmitter.ts)

## Descrição
Implementar uma classe EventEmitter fortemente tipada com suporte a listeners prioritários, emissão síncrona/assíncrona, execução única (once) e suporte a wildcards em `src/eventEmitter.ts`.

---

### Unidade 1: Módulo EventEmitter Base
- **Demanda**: `crie modulo src/eventEmitter.ts com a classe EventEmitter contendo on, once, off, emit, removeAllListeners e listenerCount`

---

### Unidade 2: Re-exportação no Index
- **Demanda**: `re-exporte EventEmitter no src/index.ts sem extensoes .ts`
