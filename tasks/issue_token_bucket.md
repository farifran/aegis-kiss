# Issue: Implementação do TokenBucket & Função obterEstadoBitmask (src/tokenBucket.ts)

## Descrição
Implementar o utilitário TokenBucket em `src/tokenBucket.ts` com suporte a temporizador de alta precisão BigInt, controle de saldo `maxTokens`, taxas de refill e exportação de status bitmask `obterEstadoBitmask`.

---

### Task 1: Módulo TokenBucket Base (Criação de `src/tokenBucket.ts`)
- **Demanda**: `crie modulo src/tokenBucket.ts com a classe TokenBucket contendo construtor, update e consume`

---

### Task 2: Função Bitmask Status em `src/tokenBucket.ts`
- **Demanda**: `adicione a funcao obterEstadoBitmask na classe ou modulo src/tokenBucket.ts`

---

### Task 3: Re-exportação no Index
- **Demanda**: `re-exporte TokenBucket e obterEstadoBitmask no src/index.ts sem extensoes .ts`
