# =========================================================
# AEGIS / PonyTail — structural physics (Go)
# Disallow unhandled panic() in production code.
# =========================================================

id: no-panic-go

language: Go

severity: error

message: >
  Do not use panic() in production Go code (PonyTail rule). Return an explicit error value (error) as the last return parameter.

rule:
  pattern: "panic($MSG)"
