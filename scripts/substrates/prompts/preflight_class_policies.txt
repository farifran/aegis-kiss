syntax|Parse/structure error. Do not nest export function/const/class inside another class. Top-level exports only; free functions AFTER the class. Close all braces; whole file must parse.
any|Remove any/as any/@ts-ignore. Use precise types or unknown + narrowing. Never silence the checker.
import|Relative imports: NodeNext .js extension (from './mod.js'). Only package.json deps or Node builtins. Builtins are globals — do not import them as npm packages.
numeric|Never mix bigint with number in one expression. Convert explicitly: BigInt(x) to widen, and keep token/byte balances as bigint end to end. Use 0n literals. Do not call Number() on a balance.
type|Fix signatures and conversions. Prefer smallest correct type fix. Do not add any. Initialize every declared property in the constructor or mark it optional.
runtime_load|Fix import paths/exports so the module loads. Relative .js specifier must match an on-disk sibling module.
other|Fix with the smallest edit; stay inside authorized files.
