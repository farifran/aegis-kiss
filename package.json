{
  "name": "aegis-harness",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Evidence governance for IDE-driven software engineering.",
  "scripts": {
    "test": "npm run aegis:test",
    "aegis:cleanup": "bash aegis --clean",
    "aegis:typecheck": "tsc --noEmit",
    "aegis:lint": "eslint .",
    "aegis:generate-schemas": "node scripts/generate_schema_validators.mjs",
    "aegis:check-generated-schemas": "node scripts/generate_schema_validators.mjs --check",
    "aegis:enforce": "bash scripts/substrates/static_gate.sh --workspace src",
    "aegis:verify-structure": "npm run aegis:typecheck && npm run aegis:lint && npm run aegis:enforce",
    "aegis:sanity": "npm run aegis:verify-structure",
    "aegis:test:governance-schemas": "bash scripts/substrates/test/test_governance_schemas.sh",
    "aegis:test:issue-contract": "bash scripts/substrates/test/test_issue_contract.sh",
    "aegis:test": "npm run aegis:check-generated-schemas && npm run aegis:test:governance-schemas && npm run aegis:test:issue-contract"
  },
  "devDependencies": {
    "@ast-grep/cli": "^0.30.0",
    "@eslint/js": "^9.0.0",
    "@types/node": "^26.4.1",
    "ajv": "^8.20.0",
    "eslint": "^9.0.0",
    "eslint-plugin-boundaries": "^4.0.0",
    "tsx": "^4.23.13",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0"
  },
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  }
}
